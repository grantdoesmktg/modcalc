'use client';
import VehicleSelector from './VehicleSelector';

export default function VehiclePickerClient() {
  // You can add an onChange handler later if you want to lift the selection up.
  return <VehicleSelector />;
}

